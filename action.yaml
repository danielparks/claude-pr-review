name: Claude PR Review
description: >
  Have Claude review and re-review pull requests with the context of all the
  comments in the pull request, including inline comments along with the nearest
  diff fragment.

  (The full diff is not included; Claude already has access to that.)

  This detects whether a re-review is needed on `synchronize` (push) by
  comparing the PR diff against a cached baseline. If the PR is the same, i.e.
  it was rebased after changes were made elsewhere in the codebase, this skips
  the Claude review.

inputs:
  anthropic-api-key:
    description: >
      Anthropic API key for token-based billing. Either this or
      claude-code-oauth-token must be specified.
  claude-code-oauth-token:
    description: >
      Claude Code OAuth token for use with a subscription account. Generate with
      `claude setup-token`. Either this or anthropic-api-key must be specified.
  initial-review-prompt:
    description: Prompt for when no prior Claude review exists.
    default: |
      Please review this pull request. The PR branch is already checked out in
      the current working directory, and the PR discussion is included below.
  re-review-prompt:
    description: Prompt for when a prior Claude review exists.
    default: |
      This pull request has been updated. The PR discussion (including your
      prior review) and a diff of what changed since your last review are
      included below.

      Respond to any new discussion.

      If there are new changes, update your feedback to reflect the current
      state of the PR:
      - Update any comments that have been addressed.
      - Flag any new issues introduced by the changes.
  prompt-suffix:
    description: >
      Appended to `initial-review-prompt` and `re-review-prompt`.
    default: |
      - Call `mcp__pr_review__add_comment` for every specific code issue you
        find. It only queues the comment — nothing is posted yet.
      - When you're done, call `mcp__pr_review__submit_review` exactly once
        so all queued comments post together as a single review. Pass
        `body` for top-level feedback that isn't tied to a specific line;
        you can call it with only `body` and no queued comments if you have
        nothing to flag inline.
      - Use `mcp__pr_review__reply_to_comment` to reply to existing diff
        comments.
      - Only post GitHub comments — don't submit review text as messages.
  additional-prompt-suffix:
    description: >
      Appended after `prompt-suffix`.

      This is useful if you just want to add text to the default prompt.
    default:
  bot-username:
    description: GitHub username of the bot; used to detect prior reviews.
    default: claude
  allowed-tools:
    description: >
      List of tools to allow Claude to use, one per line. Passed to
      `--allowedTools`.
    default: |
      mcp__pr_review__add_comment
      mcp__pr_review__submit_review
      mcp__pr_review__reply_to_comment
      Read
      Bash(find:*)
      Bash(grep:*)
      Bash(git log:*)
      Bash(git diff:*)
      Bash(git blame:*)
      Bash(gh pr diff:*)
      Bash(gh pr view:*)
      Bash(gh run list:*)
      Bash(gh run view:*)
  additional-allowed-tools:
    description: >
      List of additional tools to allow Claude to use, one per line. Added to
      `allowed-tools` and then passed to `--allowedTools`.

      This is useful if you just want to add a tool to the default tool list.
    default:
  gh-pr-render-version:
    description: Version of the `gh-pr-render` tool to use.
    default: 0.4.0

runs:
  using: composite
  steps:
    - name: Build allowed-tools list
      id: tools
      shell: bash
      env:
        ALLOWED_TOOLS: |
          ${{ inputs.allowed-tools }}
          ${{ inputs.additional-allowed-tools }}
      run: |
        # Build allowed-tools list
        set -euo pipefail

        echo "list=$(
          echo "$ALLOWED_TOOLS" \
            | grep -v '^[[:space:]]*$' \
            | tr '\n' ',' \
            | sed 's/,$//' \
          )" >> "$GITHUB_OUTPUT"

    - name: Build MCP config
      id: mcp
      shell: bash
      env:
        REPO: ${{ github.repository }}
        PR_NUMBER: ${{ github.event.pull_request.number }}
        GH_TOKEN: ${{ github.token }}
        SCRIPT_PATH: ${{ github.action_path }}/mcp-server/dist/index.js
      run: |
        # Build the --mcp-config JSON for the pr_review server (see
        # mcp-server/README.md). This is what lets Claude group inline
        # comments into a single review instead of scattering them across
        # many. dist/index.js is a committed, dependency-free bundle, so
        # this just runs it directly -- no install step needed here.
        set -euo pipefail

        CONFIG=$(jq -nc \
          --arg script "$SCRIPT_PATH" \
          --arg owner "${REPO%%/*}" \
          --arg repo "${REPO#*/}" \
          --arg pr "$PR_NUMBER" \
          --arg token "$GH_TOKEN" \
          '{
            mcpServers: {
              pr_review: {
                command: "node",
                args: [$script],
                env: {
                  GITHUB_TOKEN: $token,
                  REPO_OWNER: $owner,
                  REPO_NAME: $repo,
                  PR_NUMBER: $pr
                }
              }
            }
          }')

        echo "config=$CONFIG" >> "$GITHUB_OUTPUT"

    - name: diff cache
      uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
      with:
        path: .pr-cache
        key: pr-diff-${{ github.event.pull_request.number }}-${{ github.run_id }}
        restore-keys: |
          pr-diff-${{ github.event.pull_request.number }}-

    - name: Compute and compare diffs
      id: diff
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
        BASE_SHA: ${{ github.event.pull_request.base.sha }}
        HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        REPO: ${{ github.repository }}
        REPO_URL: "https://x-access-token:${{ github.token }}@github.com/${{ github.repository }}.git"
      run: |
        # Get the merge base SHA from the API, then fetch and diff locally.
        set -euo pipefail

        # Using the merge base (rather than BASE_SHA) ensures we only diff
        # what the PR author changed, excluding commits pushed to the base
        # branch after this branch was created.
        MERGE_BASE=$(gh api "repos/$REPO/compare/${BASE_SHA}...${HEAD_SHA}" \
          --jq '.merge_base_commit.sha')
        if [ -z "$MERGE_BASE" ]; then
          echo "::error::Failed to resolve merge base from GitHub API" >&2
          exit 1
        fi

        if ! git cat-file -e "${MERGE_BASE}^{commit}" 2>/dev/null; then
          git fetch --depth=1 "$REPO_URL" "$MERGE_BASE"
        fi
        if ! git cat-file -e "${HEAD_SHA}^{commit}" 2>/dev/null; then
          git fetch --depth=1 "$REPO_URL" "$HEAD_SHA"
        fi

        mkdir -p .pr-cache
        # Remove line numbers from @@ hunk headers and delete index lines (base
        # blob SHAs) so that a clean rebase produces an identical diff.
        # Note: binary files are excluded from the diff and won't be reviewed.
        git diff "${MERGE_BASE}..${HEAD_SHA}" \
          | grep -v '^index ' \
          | sed -e 's/^@@.*@@/@@ - - @@/' \
          > .pr-cache/new.patch

        if [ -f .pr-cache/prev.patch ]; then
          if diff .pr-cache/prev.patch .pr-cache/new.patch >/dev/null ; then
            echo PR unchanged
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo PR changed
            DELIM=$(openssl rand -hex 8)
            {
              echo "diff_section<<EOF-${DELIM}"
              echo '## Changes since last review'
              echo '```'
              diff .pr-cache/prev.patch .pr-cache/new.patch || true
              echo '```'
              echo "EOF-${DELIM}"
            } >> "$GITHUB_OUTPUT"
          fi
        fi

        cp .pr-cache/new.patch .pr-cache/prev.patch

    - name: Render PR discussion
      id: render
      if: steps.diff.outputs.skip != 'true'
      shell: bash
      env:
        GH_TOKEN: ${{ github.token }}
        REPO: ${{ github.repository }}
        PR_NUMBER: ${{ github.event.pull_request.number }}
        RENDER_VERSION: ${{ inputs.gh-pr-render-version }}
      run: |
        # Generate PR context
        set -euo pipefail

        DELIM=$(openssl rand -hex 8)
        {
          echo "pr_context<<EOF-${DELIM}"
          # Don't use local node_modules. Without `--prefix` it fails when
          # running in the gh-pr-render repo.
          npm exec --yes --prefix $(mktemp -d) \
            --package=gh-pr-render@"$RENDER_VERSION" -- \
            gh-pr-render "$REPO" "$PR_NUMBER"
          echo "EOF-${DELIM}"
        } >> "$GITHUB_OUTPUT"

    - name: Select prompt
      id: select-prompt
      if: steps.diff.outputs.skip != 'true'
      shell: bash
      env:
        BOT_USERNAME: ${{ inputs.bot-username }}
        INITIAL_PROMPT: ${{ inputs.initial-review-prompt }}
        RE_REVIEW_PROMPT: ${{ inputs.re-review-prompt }}
        PR_CONTEXT: ${{ steps.render.outputs.pr_context }}
      run: |
        # Generate correct prompt
        set -euo pipefail

        DELIM=$(openssl rand -hex 8)
        {
          echo "prompt<<EOF-${DELIM}"
          if echo "$PR_CONTEXT" | grep "^### Comment by " | grep -qF " $BOT_USERNAME "; then
            printf '%s\n' "$RE_REVIEW_PROMPT"
          else
            printf '%s\n' "$INITIAL_PROMPT"
          fi
          echo "EOF-${DELIM}"
        } >> "$GITHUB_OUTPUT"

    - uses: anthropics/claude-code-action@51ea8ea73a139f2a74ff649e3092c25a904aed7e # v1.0.123
      if: steps.diff.outputs.skip != 'true'
      with:
        anthropic_api_key: ${{ inputs.anthropic-api-key }}
        claude_code_oauth_token: ${{ inputs.claude-code-oauth-token }}
        prompt: |
          REPO: ${{ github.repository }}
          PR NUMBER: ${{ github.event.pull_request.number }}

          ${{ steps.select-prompt.outputs.prompt }}

          ${{ inputs.prompt-suffix }}
          ${{ inputs.additional-prompt-suffix }}

          ${{ steps.diff.outputs.diff_section }}

          ${{ steps.render.outputs.pr_context }}
        claude_args: --allowedTools "${{ steps.tools.outputs.list }}" --mcp-config '${{ steps.mcp.outputs.config }}'
